1.table creation using prisma in mysql(sample)

model User {
  id        Int      @id @default(autoincrement())
  name      String
  email     String   @unique
  createdAt DateTime @default(now())
}
 
run command in teriminal the table is store in the database
    
    npx prisma db push


